export interface DocumentDTO {
    id: string;
    title: string;
    content: string;
    author: string;
    createdAt: Date;
    updatedAt: Date;
    // Add other properties for document metadata, categories, tags, etc.
  }
  